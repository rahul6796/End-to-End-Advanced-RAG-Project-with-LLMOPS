[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "multi_docs_chat"
version = "0.1.0"

[tool.setuptools.packages.find]
include = ["multi_docs_chat*"]